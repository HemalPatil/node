{
	"name": "test-mcdc",
	"version": "1.0.0",
	"description": "Test package for MCDC",
	"type": "module",
	"scripts": {
		"proof": "node --version && node --test --experimental-test-coverage --test-reporter=spec --test-reporter=./coverage-listener.mts --test-reporter-destination=stdout --test-reporter-destination=stdout --test-coverage-exclude='!test/mcdc/**' --test-coverage-exclude='**/*.test.*' ./proof.test.mts"
	},
	"engines": {
		"node": ">=26.5.0"
	},
	"devDependencies": {
		"@types/node": "^26.6.2"
	}
}
